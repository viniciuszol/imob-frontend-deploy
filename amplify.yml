version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

rewrites:
  - source: /api/<*>
    target: http://54.207.198.211:8000/<*>
    status: 200
  - source: /<*>
    target: /index.html
    status: 200
